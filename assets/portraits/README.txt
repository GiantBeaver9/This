Character-select portraits — drop full-res illustrations here, named by character id:
  tactical.png    shotgunner.png    werewolf.png    underdog.png
PNG, any size (1024x1024 fine). They show on the CHOOSE YOUR FIGHTER screen live —
no rebuild. White backgrounds are fine; ScaleToFit letterboxes them into the card.
To turn one into an in-game pixel sprite: tools/sprite_from_art.py <in> <out> --height 64
